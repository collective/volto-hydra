# Sphinx build output
_build/

# Generated demo video binaries — regenerated by `pnpm demo:encode`.
# The Plone File metadata (data.json) is committed; the mp4 itself
# isn't, so a fresh checkout has the shape but needs to recapture
# before deploy.
_static/hydra-demo.mp4
content/content/content/docs/static/hydra-demo/file/hydra-demo.mp4
