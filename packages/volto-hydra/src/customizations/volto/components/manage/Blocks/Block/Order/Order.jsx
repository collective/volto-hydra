/**
 * Order widget customization for Volto Hydra.
 * Returns null because the ChildBlocksWidget replaces this functionality
 * in the unified hierarchical sidebar (PLIP 6569).
 */

// eslint-disable-next-line no-unused-vars
const Order = (props) => null;

export default Order;
