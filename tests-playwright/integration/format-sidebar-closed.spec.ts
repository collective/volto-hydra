/**
 * Integration tests for formatting when sidebar is closed in Volto Hydra.
 *
 * Tests that format buttons handle the case when the sidebar widget is unmounted.
 */
import { test, expect } from '@playwright/test';
import { AdminUIHelper } from '../helpers/AdminUIHelper';

test.describe('Formatting with Sidebar Closed', () => {
  test('format button still works when sidebar is closed', async ({ page }) => {
    //TODO

    // For now, mark as pending implementation
    test.skip();
  });
});
