name: Build and test

on:
  push:

jobs:
  # ── Build Volto and upload artifact ──────────────────────────────────────
  build:
    runs-on: 'ubuntu-latest'
    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup pnpm
        run: |
          npm install -g corepack
          corepack cache clean
          corepack enable
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      - name: Get pnpm store directory
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV
      - uses: actions/cache@v4
        name: Setup pnpm cache
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-
      - name: Install dependencies
        run: |
          pnpm dlx mrs-developer missdev --no-config --fetch-https
          pnpm install --frozen-lockfile
      - name: Run hydra-js unit tests
        run: cd packages/hydra-js && pnpm test
      - name: Build Volto for production
        run: pnpm build
        env:
          RAZZLE_API_PATH: http://localhost:8888
          RAZZLE_DEFAULT_IFRAME_URL: http://localhost:8888,http://localhost:3003,http://localhost:3004,http://localhost:3005,http://localhost:3006,http://localhost:3007,http://localhost:3008
          VOLTOCONFIG: ${{ github.workspace }}/volto.config.js
      - name: Upload Volto build
        uses: actions/upload-artifact@v4
        with:
          name: volto-build
          path: build/
          retention-days: 1

  # ── Bridge + unit tests (no Volto needed) ────────────────────────────────
  test-bridge-unit:
    runs-on: 'ubuntu-latest'
    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup pnpm
        run: |
          npm install -g corepack
          corepack cache clean
          corepack enable
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      - name: Get pnpm store directory
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV
      - uses: actions/cache@v4
        name: Setup pnpm cache
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-
      - name: Install dependencies
        run: |
          pnpm dlx mrs-developer missdev --no-config --fetch-https
          pnpm install --frozen-lockfile
      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium
      - name: Start servers
        run: |
          node tests-playwright/fixtures/mock-api-server.js &
          cd docs/blocks/test-react && npx vite --port 3004 --strictPort &
          cd docs/blocks/test-svelte && npx vite --port 3005 --strictPort &
          cd docs/blocks/test-vue && npx vite --port 3006 --strictPort &
          sleep 3
      - name: Wait for servers
        run: |
          for port in 8888 3004 3005 3006; do
            echo "Waiting for port $port..."
            for i in {1..30}; do
              curl -s http://localhost:$port > /dev/null && break
              sleep 2
            done
          done
          echo "All servers ready"
      - name: Run tests
        run: pnpm exec playwright test --project=unit --project=mock --project=react --project=svelte --project=vue --reporter=github,list,html
        env:
          CI: true
          NO_WEBSERVER: true
          SKIP_VOLTO_CHECK: true
          COVERAGE: true
      - name: Upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-bridge-unit
          path: |
            playwright-report/
            test-results/**/*.png
          retention-days: 7
      - name: Upload coverage
        if: always()
        continue-on-error: true
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/lcov.info
          flags: bridge-unit
          fail_ci_if_error: false

  # ── Admin integration tests (mock frontend) ─────────────────────────────
  test-admin-mock:
    needs: build
    runs-on: 'ubuntu-latest'
    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup pnpm
        run: |
          npm install -g corepack
          corepack cache clean
          corepack enable
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      - name: Get pnpm store directory
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV
      - uses: actions/cache@v4
        name: Setup pnpm cache
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-
      - name: Install dependencies
        run: |
          pnpm dlx mrs-developer missdev --no-config --fetch-https
          pnpm install --frozen-lockfile
      - name: Download Volto build
        uses: actions/download-artifact@v4
        with:
          name: volto-build
          path: build/
      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium
      - name: Start servers
        run: |
          node tests-playwright/fixtures/mock-api-server.js &
          PORT=3001 RAZZLE_API_PATH=http://localhost:8888 \
            RAZZLE_DEFAULT_IFRAME_URL=http://localhost:8888,http://localhost:3003 \
            VOLTOCONFIG=${{ github.workspace }}/volto.config.js \
            pnpm start:prod &
          sleep 3
      - name: Wait for servers
        run: |
          echo "Waiting for mock API (8888)..."
          for i in {1..60}; do curl -s http://localhost:8888/health > /dev/null && break; sleep 2; done
          echo "Waiting for Volto (3001)..."
          for i in {1..60}; do curl -s http://localhost:3001 > /dev/null && break; sleep 2; done
          echo "All servers ready"
      - name: Run tests
        run: pnpm exec playwright test --project=admin-mock --reporter=github,list,html
        env:
          CI: true
          NO_WEBSERVER: true
          USE_PREBUILT: true
          COVERAGE: true
      - name: Upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-admin-mock
          path: |
            playwright-report/
            test-results/**/*.png
          retention-days: 7
      - name: Upload coverage
        if: always()
        continue-on-error: true
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/lcov.info
          flags: admin-mock
          fail_ci_if_error: false

  # ── Nuxt tests (bridge + integration) ───────────────────────────────────
  test-nuxt:
    needs: build
    runs-on: 'ubuntu-latest'
    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup pnpm
        run: |
          npm install -g corepack
          corepack cache clean
          corepack enable
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      - name: Get pnpm store directory
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV
      - uses: actions/cache@v4
        name: Setup pnpm cache
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-
      - name: Install dependencies
        run: |
          pnpm dlx mrs-developer missdev --no-config --fetch-https
          pnpm install --frozen-lockfile
      - name: Download Volto build
        uses: actions/download-artifact@v4
        with:
          name: volto-build
          path: build/
      - name: Install and build Nuxt
        run: cd examples/nuxt-blog-starter && pnpm install --ignore-workspace && pnpm run build:test
      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium
      - name: Start servers
        run: |
          node tests-playwright/fixtures/mock-api-server.js &
          PORT=3001 RAZZLE_API_PATH=http://localhost:8888 \
            RAZZLE_DEFAULT_IFRAME_URL=http://localhost:8888,http://localhost:3003 \
            VOLTOCONFIG=${{ github.workspace }}/volto.config.js \
            pnpm start:prod &
          cd examples/nuxt-blog-starter && pnpm run preview:test &
          sleep 3
      - name: Wait for servers
        run: |
          echo "Waiting for mock API (8888)..."
          for i in {1..60}; do curl -s http://localhost:8888/health > /dev/null && break; sleep 2; done
          echo "Waiting for Volto (3001)..."
          for i in {1..60}; do curl -s http://localhost:3001 > /dev/null && break; sleep 2; done
          echo "Waiting for Nuxt (3003)..."
          for i in {1..90}; do curl -s http://localhost:3003 > /dev/null && break; sleep 2; done
          echo "All servers ready"
      - name: Run tests
        run: pnpm exec playwright test --project=nuxt --project=admin-nuxt --project=nuxt-specific --reporter=github,list,html
        env:
          CI: true
          NO_WEBSERVER: true
          USE_PREBUILT: true
          COVERAGE: true
      - name: Upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-nuxt
          path: |
            playwright-report/
            test-results/**/*.png
          retention-days: 7
      - name: Upload coverage
        if: always()
        continue-on-error: true
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/lcov.info
          flags: nuxt
          fail_ci_if_error: false

  # ── Next.js + F7 bridge tests (no Volto needed) ─────────────────────────
  test-nextjs-f7:
    runs-on: 'ubuntu-latest'
    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup pnpm
        run: |
          npm install -g corepack
          corepack cache clean
          corepack enable
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'
      - name: Get pnpm store directory
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV
      - uses: actions/cache@v4
        name: Setup pnpm cache
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-
      - name: Install dependencies
        run: |
          pnpm dlx mrs-developer missdev --no-config --fetch-https
          pnpm install --frozen-lockfile
      - name: Build Next.js example
        run: cd examples/hydra-nextjs && pnpm install --ignore-workspace && pnpm run build:test
        env:
          NEXT_PUBLIC_BACKEND_BASE_URL: http://localhost:8888
      - name: Install F7 example
        run: cd examples/hydra-vue-f7 && pnpm install --ignore-workspace
      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium
      - name: Start servers
        run: |
          node tests-playwright/fixtures/mock-api-server.js &
          cd examples/hydra-nextjs && pnpm run start:test &
          cd examples/hydra-vue-f7 && cp ../../packages/hydra-js/hydra.js ./src/js/hydra.js && \
            VITE_API_BASE_URL=http://localhost:8888 npx vite --port 3008 --strictPort --config vite.config.test.js &
          sleep 3
      - name: Wait for servers
        run: |
          for port in 8888 3007 3008; do
            echo "Waiting for port $port..."
            for i in {1..30}; do
              curl -s http://localhost:$port > /dev/null && break
              sleep 2
            done
          done
          echo "All servers ready"
      - name: Run tests
        run: pnpm exec playwright test --project=nextjs --project=f7 --reporter=github,list,html
        env:
          CI: true
          NO_WEBSERVER: true
          SKIP_VOLTO_CHECK: true
          COVERAGE: true
      - name: Upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-nextjs-f7
          path: |
            playwright-report/
            test-results/**/*.png
          retention-days: 7
      - name: Upload coverage
        if: always()
        continue-on-error: true
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/lcov.info
          flags: nextjs-f7
          fail_ci_if_error: false
