{
  "_comment": "Conversion-time only. This says how to LAY OUT a block as markdown; the markdown that comes out records the decision in its own attributes, so nothing needs this file to read it. Values are literally what gets written into the attribute.",

  "heading": { "construct": "heading", "fields": { "heading": "${text}", "tag": "h${level}" } },
  "image":   { "construct": "image",   "fields": { "url": "${src}", "alt": "${alt}" } },

  "codeExample": {
    "sections": { "field": "tabs", "at": "h3",
                  "item": { "label": "${text}", "language": "${lang}", "code": "${code}" } }
  },
  "accordion": {
    "sections": { "field": "panels", "at": "h2",
                  "item": { "title": "${text}", "blocks": "${body}" } }
  }
}
